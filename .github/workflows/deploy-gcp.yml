name: 🚀 Deploy MONPEC para Google Cloud

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Ambiente de deploy'
        required: true
        default: 'production'
        type: choice
        options:
        - production
        - staging
      deploy_type:
        description: 'Tipo de deploy'
        required: true
        default: 'cloud-run'
        type: choice
        options:
        - cloud-run
        - app-engine

env:
  GCP_PROJECT_ID: monpec-sistema-rural
  GCP_REGION: us-central1
  SERVICE_NAME: monpec-app

jobs:
  # Job 1: Testes e validações
  test:
    name: 🧪 Testes e Validações
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4

    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 📦 Instalar dependências
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements_gcp.txt

    - name: 🔧 Configurar Django
      run: |
        python manage.py collectstatic --noinput --settings=sistema_rural.settings_gcp_deploy
        python manage.py check --settings=sistema_rural.settings_gcp_deploy

    - name: ✅ Executar correções GCP
      run: |
        python corrigir_exportacao_gcp.py
        python corrigir_migracoes_gcp.py

    - name: 🧪 Testes básicos
      run: |
        DJANGO_SETTINGS_MODULE=sistema_rural.settings_gcp_deploy python -c "import django; django.setup(); print('✅ Django OK')"
        DJANGO_SETTINGS_MODULE=sistema_rural.settings_gcp_deploy python -c "from gestao_rural.utils.export_utils import exportar_excel_seguro; print('✅ Export utils OK')"

  # Job 2: Build da imagem Docker
  build:
    name: 🐳 Build Docker Image
    runs-on: ubuntu-latest
    needs: test

    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4

    - name: 🔐 Autenticar no Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: 🏗️ Configurar Docker
      run: |
        gcloud auth configure-docker --quiet
        gcloud auth list
        echo "🔍 Verificando projeto atual..."
        gcloud config get-value project

    - name: 🐳 Build da imagem
      run: |
        docker build -f Dockerfile.gcp -t gcr.io/${{ env.GCP_PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }} .
        docker build -f Dockerfile.gcp -t gcr.io/${{ env.GCP_PROJECT_ID }}/${{ env.SERVICE_NAME }}:latest .

    - name: 📤 Push da imagem (com diagnóstico)
      run: |
        echo "🔄 Tentando push da imagem Docker..."
        if docker push gcr.io/${{ env.GCP_PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }}; then
          echo "✅ Push SHA tag: OK"
          docker push gcr.io/${{ env.GCP_PROJECT_ID }}/${{ env.SERVICE_NAME }}:latest || echo "⚠️ Latest tag falhou, mas SHA foi enviado"
        else
          echo "❌ ERRO CRÍTICO: Service Account SEM permissões suficientes!"
          echo ""
          echo "🔴 CAUSA: Service Account 'github-actions-deploy@monpec-sistema-rural.iam.gserviceaccount.com'"
          echo "🔴 PROBLEMA: Não consegue habilitar APIs nem fazer upload para GCR"
          echo ""
          echo "🟢 SOLUÇÃO 1 (RECOMENDADA): Usar conta Owner/Admin"
          echo "   1. Vá ao GCP IAM: https://console.cloud.google.com/iam-admin/iam"
          echo "   2. Projeto: monpec-sistema-rural"
          echo "   3. Encontre: github-actions-deploy@..."
          echo "   4. ALTERAR PAPEL PARA: 'Editor' ou 'Owner'"
          echo "   5. Salvar e aguardar 2-3 minutos"
          echo ""
          echo "🟡 SOLUÇÃO 2: Criar nova Service Account"
          echo "   1. GCP IAM > Service Accounts > Criar"
          echo "   2. Nome: github-deploy-admin"
          echo "   3. Papel: Editor (ou Owner)"
          echo "   4. Criar chave JSON"
          echo "   5. Atualizar secret GCP_SA_KEY no GitHub"
          echo ""
          echo "📞 CONTATO: (67) 99968-8561 | l.moncaosilva@gmail.com"
          exit 1
        fi

  # Job 3: Deploy para Cloud Run
  deploy-cloud-run:
    name: ☁️ Deploy Cloud Run
    runs-on: ubuntu-latest
    needs: build
    if: github.event.inputs.deploy_type == 'cloud-run' || github.event_name == 'push'

    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4

    - name: 🔐 Autenticar no Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: 🚀 Deploy para Cloud Run
      run: |
        echo "🗑️ Removendo serviço existente (se houver)..."
        gcloud run services delete ${{ env.SERVICE_NAME }} --region=${{ env.GCP_REGION }} --quiet || echo "⚠️ Serviço não existia ou já foi removido"

        echo "🚀 Fazendo deploy limpo..."
        gcloud run deploy ${{ env.SERVICE_NAME }} \
          --image gcr.io/${{ env.GCP_PROJECT_ID }}/${{ env.SERVICE_NAME }}:${{ github.sha }} \
          --platform managed \
          --region ${{ env.GCP_REGION }} \
          --allow-unauthenticated \
          --port 8080 \
          --memory 2Gi \
          --cpu 1 \
          --max-instances 10 \
          --min-instances 1 \
          --set-env-vars="DJANGO_SETTINGS_MODULE=sistema_rural.settings_gcp_deploy" \
          --set-env-vars="SECRET_KEY=${{ secrets.DJANGO_SECRET_KEY }}" \
          --set-env-vars="MERCADOPAGO_ACCESS_TOKEN=${{ secrets.MERCADOPAGO_ACCESS_TOKEN }}" \
          --set-env-vars="EMAIL_HOST_PASSWORD=${{ secrets.EMAIL_HOST_PASSWORD }}" \
          --set-env-vars="CONSULTOR_TELEFONE=${{ secrets.CONSULTOR_TELEFONE }}" \
          --add-cloudsql-instances ${{ env.GCP_PROJECT_ID }}:${{ env.GCP_REGION }}:monpec-db

    - name: 📍 Obter URL do serviço
      run: |
        SERVICE_URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} --region=${{ env.GCP_REGION }} --format="value(status.url)")
        echo "🌐 URL do serviço: $SERVICE_URL"
        echo "SERVICE_URL=$SERVICE_URL" >> $GITHUB_ENV

    - name: 🧪 Testar deploy
      run: |
        sleep 30  # Aguardar serviço ficar pronto
        if curl -f --max-time 30 ${{ env.SERVICE_URL }}/health/; then
          echo "✅ Deploy bem-sucedido!"
        else
          echo "❌ Falha no teste do deploy"
          exit 1
        fi

    - name: 📢 Notificar deploy
      if: success()
      run: |
        echo "🎉 Deploy realizado com sucesso!"
        echo "📱 WhatsApp: ${{ secrets.CONSULTOR_TELEFONE }}"
        echo "🌐 URL: ${{ env.SERVICE_URL }}"

  # Job 4: Deploy alternativo para App Engine
  deploy-app-engine:
    name: 🌐 Deploy App Engine
    runs-on: ubuntu-latest
    needs: build
    if: github.event.inputs.deploy_type == 'app-engine'

    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4

    - name: 🔐 Autenticar no Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: 🚀 Deploy para App Engine
      run: |
        gcloud app deploy app.yaml --quiet \\
          --version ${{ github.sha }} \\
          --no-promote

    - name: 🧪 Testar deploy App Engine
      run: |
        if curl -f --max-time 30 https://monpec-sistema-rural.uc.r.appspot.com/health/; then
          echo "✅ Deploy App Engine bem-sucedido!"
        else
          echo "❌ Falha no teste do deploy App Engine"
          exit 1
        fi

  # Job 5: Pós-deploy (executar migrações remotas)
  post-deploy:
    name: 🔧 Pós-deploy
    runs-on: ubuntu-latest
    needs: [deploy-cloud-run, deploy-app-engine]
    if: success()

    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4

    - name: 🔐 Autenticar no Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: 🗄️ Executar migrações remotas
      run: |
        # Cloud Run - executar migrações via Cloud Run job ou SSH
        echo "🔄 Executando migrações remotas..."
        # TODO: Implementar execução remota de migrações

    - name: 📧 Notificar conclusão
      run: |
        echo "🎉 DEPLOY MONPEC CONCLUÍDO COM SUCESSO!"
        echo "📱 WhatsApp configurado: ${{ secrets.CONSULTOR_TELEFONE }}"
        echo "📧 Email: l.moncaosilva@gmail.com"
        echo "🌐 Ambiente: ${{ github.event.inputs.environment || 'production' }}"