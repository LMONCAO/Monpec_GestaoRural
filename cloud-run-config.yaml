# Configuração para deploy no Google Cloud Run
# Arquivo de referência - usar os scripts em deploy/gcp/

# Docker Build (executar na raiz do projeto)
# docker build -t gcr.io/monpec-sistema-rural/monpec:latest .

# Push para Google Container Registry
# docker push gcr.io/monpec-sistema-rural/monpec:latest

# Deploy para Cloud Run
# gcloud run deploy monpec \
#   --image gcr.io/monpec-sistema-rural/monpec:latest \
#   --platform managed \
#   --region us-central1 \
#   --allow-unauthenticated \
#   --set-env-vars "DJANGO_SETTINGS_MODULE=sistema_rural.settings_gcp" \
#   --set-env-vars "DEBUG=False" \
#   --set-env-vars "SECRET_KEY=rfzjy1t-wda0oi+p!_4!4-n-1a60" \
#   --set-env-vars "GOOGLE_CLOUD_PROJECT=monpec-sistema-rural" \
#   --set-env-vars "DB_NAME=monpec_db" \
#   --set-env-vars "DB_USER=monpec_user" \
#   --set-env-vars "DB_PASSWORD=L6171r12@@jjms" \
#   --set-env-vars "DB_HOST=34.9.51.178" \
#   --set-env-vars "DB_PORT=5432" \
#   --set-env-vars "CLOUD_SQL_CONNECTION_NAME=monpec-sistema-rural:us-central1:monpec-db" \
#   --add-cloudsql-instances monpec-sistema-rural:us-central1:monpec-db \
#   --memory 1Gi \
#   --cpu 1 \
#   --max-instances 10 \
#   --timeout 300 \
#   --concurrency 80

# Verificar se está rodando
# gcloud run services list

# Verificar logs
# gcloud run services logs read monpec --region=us-central1