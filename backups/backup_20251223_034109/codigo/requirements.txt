Django==4.2.7
Pillow>=10.1.0
reportlab==4.0.4
openpyxl==3.1.2
matplotlib==3.7.2
numpy==1.24.3
python-decouple==3.8
psycopg2-binary==2.9.9
PyPDF2>=3.0.0
pdfplumber>=0.10.0
requests>=2.31.0  # Para integração com APIs (consulta CPF/CNPJ, CEP, etc.)
google-auth>=2.23.0  # Para autenticação OAuth2 com Google
google-auth-oauthlib>=1.1.0  # Para fluxo OAuth2
google-auth-httplib2>=0.1.1  # Para requisições HTTP com OAuth2
pyOpenSSL>=23.0.0  # Para certificados digitais
lxml>=4.9.0  # Para processamento XML
zeep>=4.2.0  # Cliente SOAP para comunicação com SEFAZ
xmlsec>=1.3.13  # Para assinatura digital XML

# Integração Fiscal - Sintegra e Receita Federal
# Bibliotecas instaladas para trabalhar com SPED e análise de arquivos fiscais
spedpytools>=0.1.1  # Para visualização e análise de arquivos SPED (EFD, ECD, etc.)
sped-extractor>=1.0.5  # Para extrair informações dos manuais SPED
python-sped>=1.1.4  # Biblioteca base para SPED (incluída via sped-extractor)
spedpy>=1.2.3  # Biblioteca para processamento de SPED (incluída via spedpytools)

# Nota: As bibliotecas pysintegra e erpbrasil.sped não estão disponíveis no PyPI
# Para geração de arquivos Sintegra e SPED, recomenda-se:
# 1. Usar a estrutura base implementada em gestao_rural/services/
# 2. Ou integrar com APIs de terceiros (Focus NFe, NFe.io, etc.)
# 3. Ou desenvolver geradores customizados baseados nos manuais oficiais

# Emissão de NF-e Direta com SEFAZ (Opcional - descomente se for usar)
# pynfe>=0.1.0  # Biblioteca completa para emissão de NF-e diretamente com SEFAZ
# pytrustnfe>=0.1.0  # Alternativa: Biblioteca focada em comunicação com SEFAZ

