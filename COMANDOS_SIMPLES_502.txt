# === COMANDOS ULTRA SIMPLES ===
# Copie e cole cada linha individualmente no Console Web

pkill -9 python

cd /var/www/monpec.com.br

# Recrie o urls.py básico (copie TUDO de uma vez):
cat > gestao_rural/urls.py << 'END'
from django.urls import path
from . import views

app_name = 'gestao_rural'
urlpatterns = [
    path('', views.dashboard, name='dashboard'),
    path('propriedades/', views.propriedades_lista, name='propriedades_lista'),
    path('logout/', views.logout_view, name='logout'),
]
END

# Teste se está funcionando:
python manage.py check

# Se deu OK, inicie:
source venv/bin/activate
python manage.py runserver 127.0.0.1:8000

# === SE AINDA NÃO FUNCIONAR ===
# Pare tudo novamente:
# pkill -9 python

# E use este urls.py ainda mais básico:
# echo "from django.urls import path
# from . import views
# app_name = 'gestao_rural'
# urlpatterns = [path('', views.dashboard, name='dashboard')]" > gestao_rural/urls.py
